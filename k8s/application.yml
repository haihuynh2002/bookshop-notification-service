spring:
  flyway:
    url: jdbc:postgresql://bookshop-postgres/bookshopdb_notification
  r2dbc:
    url: r2dbc:postgresql://bookshop-postgres/bookshopdb_notification
  rabbitmq:
    host: bookshop-rabbitmq
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://bookshop-keycloak/realms/PolarBookshop